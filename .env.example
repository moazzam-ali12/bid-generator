# Qwen / OpenAI-compatible API settings
QWEN_API_KEY=replace_me
QWEN_BASE_URL=https://replace-me.example.com/v1
QWEN_MODEL=replace_me_model_name

# Model call settings
TEMPERATURE=0.1
MAX_TOKENS=6000

# If you want to disable keyword filtering and send more text (may hit context limits)
DISABLE_KEYWORD_FILTER=false
